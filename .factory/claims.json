[
  {
    "id": "real-agency-records",
    "claim": "Creates saved agency job chains separately from the demo fixtures",
    "where": "landing agency workspace section, /start, README",
    "test": "cargo test --manifest-path server/Cargo.toml --test agency claim_real_agency_records_persist_without_demo_fixtures --locked",
    "sandbox": "fresh application state; create an agency; assert its register starts empty, create a chain, and reload it"
  },
  {
    "id": "real-tenant-isolation",
    "claim": "One agency cannot read another agency's job chains",
    "where": "privacy, real agency API",
    "test": "cargo test --manifest-path server/Cargo.toml --test agency claim_real_agencies_are_tenant_isolated --locked",
    "sandbox": "create two agency sessions; create a chain in one; assert the other register is empty"
  },
  {
    "id": "real-shared-replica-persistence",
    "claim": "Saved agency workspaces persist across application replicas through a shared durable store",
    "where": "README, runtime architecture",
    "test": "cargo test --manifest-path server/Cargo.toml --test agency claim_real_agency_survives_replica_handoff_in_shared_persistence --locked",
    "sandbox": "two application states share a durable agency store; create an agency and job on one; read the saved job from the other"
  },
  {
    "id": "agency-role-projection",
    "claim": "Producer sessions cannot view subcontractor costs or derived margin totals, or add financial commitments",
    "where": "real agency API and privacy policy",
    "test": "cargo test --manifest-path server/Cargo.toml --test agency claim_agency_roles_hide_rates_and_block_financial_writes --locked",
    "sandbox": "owner creates a chain and producer session; assert costs and derived margin totals are absent and a cost write returns 403"
  },
  {
    "id": "agency-client-identity-projection",
    "claim": "Producer and viewer sessions cannot obtain contracting-client or end-client identities",
    "where": "team role controls, privacy policy, real agency API",
    "test": "cargo test --manifest-path server/Cargo.toml --test agency claim_restricted_roles_hide_client_identities_in_list_and_detail --locked",
    "sandbox": "owner creates a chain with unique client identities; open producer and viewer sessions; assert list and detail responses omit both fields and values"
  },
  {
    "id": "real-csv-import",
    "claim": "Maps and previews CSV rows before importing job chains into a saved agency workspace",
    "where": "real job register, /app/import, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:real-csv-import",
    "sandbox": "fresh browser; create an agency; load the bundled CSV; preview two valid rows; import and reload both saved jobs"
  },
  {
    "id": "real-data-export",
    "claim": "Exports saved agency job chains as local CSV or JSON downloads",
    "where": "real job register, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:real-data-export",
    "sandbox": "fresh browser; create an agency and job; download CSV and JSON; assert saved values are present and no export request occurs"
  },
  {
    "id": "real-beta-no-billing",
    "claim": "The saved agency workspace beta has no checkout or paid plan",
    "where": "landing first-screen facts, saved workspace section, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:real-beta-no-billing",
    "sandbox": "fresh landing page; assert free-beta wording, no pricing navigation, no checkout control, and no billing-provider request"
  },
  {
    "id": "m1-chain-math",
    "claim": "Shows client commitment, committed subcontractor cost, and expected margin in one job chain",
    "where": "landing preview, demo margin check, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-chain-math",
    "sandbox": "fresh /demo workspace; open Autumn launch films; assert $24,000 - $14,500 = $9,500 and 39.6%"
  },
  {
    "id": "m1-margin-risk",
    "claim": "Names the committed cost that puts expected margin below the job's floor",
    "where": "demo job register, margin check, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-margin-risk",
    "sandbox": "fresh /demo workspace; add a $6,000 commitment; assert the alert names it and reports a $1,300 floor shortfall"
  },
  {
    "id": "m1-linked-status",
    "claim": "Keeps scope approval and client invoice milestone status beside the same job",
    "where": "landing steps, demo job workspace, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-linked-status",
    "sandbox": "fresh /demo workspace; approve scope and mark its client invoice milestone sent; reload and assert both states remain"
  },
  {
    "id": "m1-sample-workflow",
    "claim": "The demo creates a job chain, adds a cost, approves scope, updates a client invoice milestone status, and resets the sample",
    "where": "README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-sample-workflow",
    "sandbox": "fresh /demo workspace; perform every named action and assert reset restores exactly three original jobs"
  },
  {
    "id": "m1-demo-fixtures",
    "claim": "The demo starts with three original fictional Northline Studio jobs",
    "where": "landing limits, README, demo contract",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-demo-fixtures",
    "sandbox": "fresh /demo workspace; assert the three exact fixture IDs and names plus the repository provenance record"
  },
  {
    "id": "m1-demo-no-account",
    "claim": "The sample demo needs no account",
    "where": "landing first-screen facts, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-demo-no-account",
    "sandbox": "fresh browser context; open /?demo=1; assert the seeded register works without CIAM, billing, or an account gate"
  },
  {
    "id": "m1-demo-reset",
    "claim": "Demo changes are discarded when the sample is reset",
    "where": "landing facts, demo banner, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-demo-reset",
    "sandbox": "fresh /demo workspace; add a cost, reset, assert original values return, and assert the old workspace is rejected"
  },
  {
    "id": "m1-demo-isolation-expiry",
    "claim": "Each demo workspace is isolated and expires within 24 hours",
    "where": "landing limits, privacy, terms, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-demo-isolation-expiry",
    "sandbox": "create two API contexts; mutate one; assert the other is unchanged and expiry is no more than 24 hours"
  },
  {
    "id": "m1-public-privacy",
    "claim": "The public and demo flow makes no cross-origin requests and loads no analytics",
    "where": "privacy, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-public-privacy",
    "sandbox": "record every request through landing, demo, and a mutation; assert only the product origin is used"
  },
  {
    "id": "m1-csv-import",
    "claim": "Maps CSV columns and previews every row before importing valid job chains",
    "where": "demo import, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-csv-import",
    "sandbox": "fresh /demo/import; load the bundled CSV; assert two dry-run rows and no errors; import and assert both jobs appear"
  },
  {
    "id": "m1-data-export",
    "claim": "Exports all current demo job chains as local CSV or JSON downloads",
    "where": "demo register, README",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-data-export",
    "sandbox": "fresh /demo workspace; download CSV and JSON; assert headings, three records, exact fixture ID, and no export request"
  },
  {
    "id": "m1-product-boundaries",
    "claim": "The demo does not calculate tax, decide worker status, collect money, or send invoices",
    "where": "landing limits, terms",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:m1-product-boundaries",
    "sandbox": "inspect demo controls and response keys; assert prohibited capabilities are absent while client invoice milestone status marking remains"
  },
  {
    "id": "real-workspace-onboarding",
    "claim": "Creates a saved agency workspace from the landing page",
    "where": "landing agency workspace section, /start",
    "test": "npx playwright test tests/e2e/m1.spec.ts --grep @claim:real-workspace-onboarding",
    "sandbox": "fresh browser; open /start from landing; create an agency; assert the empty saved register appears"
  },
  {
    "id": "m1-demo-cookie",
    "claim": "A host-only HttpOnly cookie identifies the demo workspace and is Secure on HTTPS",
    "where": "privacy, demo contract",
    "test": "cargo test --manifest-path server/Cargo.toml --test demo_api claim_demo_cookie_is_scoped_and_secure_on_https --locked",
    "sandbox": "provision through simulated HTTPS ingress; assert Secure, HttpOnly, SameSite=Lax, and no Domain attribute"
  },
  {
    "id": "m1-shared-replica-persistence",
    "claim": "Demo workspaces persist across application replicas through a shared durable store",
    "where": "README, demo contract",
    "test": "cargo test --manifest-path server/Cargo.toml --test demo_api workspace_survives_replica_handoff_in_shared_persistence --locked",
    "sandbox": "two application states share a durable store; create on one, read and mutate on the other, then read the change on the first"
  },
  {
    "id": "m1-port-only-startup",
    "claim": "The server starts with only PORT and defaults to port 8080",
    "where": "README",
    "test": "cargo test --manifest-path server/Cargo.toml --test runtime_contract claim_runtime_starts_with_only_port --locked",
    "sandbox": "spawn the built server with a cleared environment and only PORT, then assert /health returns 200"
  },
  {
    "id": "m1-security-headers",
    "claim": "Web responses include the product security-header policy",
    "where": "factory product contract",
    "test": "cargo test --manifest-path server/Cargo.toml --test health claim_security_headers_are_sent --locked",
    "sandbox": "request landing and assert CSP, HSTS, MIME, referrer, permissions, and frame protections"
  },
  {
    "id": "m1-api-rate-limits",
    "claim": "Demo API routes enforce shared request limits using the ingress client address",
    "where": "README, demo contract",
    "test": "cargo test --manifest-path server/Cargo.toml --test demo_api global_and_provision_limits_use_forwarded_ip_and_send_retry_after --locked",
    "sandbox": "alternate requests across two replicas; exceed allowances; assert 429 and Retry-After"
  },
  {
    "id": "m1-idempotent-creates",
    "claim": "Retrying the same create returns its saved result without adding another record",
    "where": "README",
    "test": "cargo test --manifest-path server/Cargo.toml --test demo_api concurrent_idempotent_retries_stay_available_and_create_one_cost --locked",
    "sandbox": "send 12 concurrent retries across two replicas; assert one 201, eleven 200 responses, and one new cost"
  },
  {
    "id": "m1-problem-details",
    "claim": "Rejected requests return structured problem details",
    "where": "README",
    "test": "cargo test --manifest-path server/Cargo.toml --test demo_api claim_problem_details_are_structured_and_bounded --locked",
    "sandbox": "send invalid and oversized requests; assert problem media type, stable code, request ID, field guidance, and 64 KiB bound"
  },
  {
    "id": "m1-demo-no-store",
    "claim": "Every demo API response, including errors, uses Cache-Control: no-store",
    "where": "README, demo contract",
    "test": "cargo test --manifest-path server/Cargo.toml --test demo_api claim_every_demo_response_uses_no_store --locked",
    "sandbox": "assert a successful response and a rejected response both carry Cache-Control: no-store"
  },
  {
    "id": "m1-money-integrity",
    "claim": "Money uses integer cents and percentage floors round upward to the next cent when needed",
    "where": "README, demo contract",
    "test": "cargo test --manifest-path server/Cargo.toml --lib claim_money_integrity_uses_integer_minor_units_and_rounds_up --locked",
    "sandbox": "calculate a 25% floor on 10,001 cents; assert integer inputs and outputs plus the 2,501-cent upward result"
  },
  {
    "id": "m1-build-identity",
    "claim": "The health response reports the build SHA supplied during the image build",
    "where": "README",
    "test": "BUILD_SHA=claim-build-0123456789 cargo test --manifest-path server/Cargo.toml --test health claim_health_reports_build_identity --locked",
    "sandbox": "compile with claim-build-0123456789 and assert /health returns that exact identity"
  },
  {
    "id": "m1-true-404",
    "claim": "Unknown page addresses return HTTP 404",
    "where": "routing contract",
    "test": "cargo test --manifest-path server/Cargo.toml --test health claim_unknown_routes_return_true_404 --locked",
    "sandbox": "request an unknown path and assert HTTP 404 with the designed recovery page"
  },
  {
    "id": "m1-asset-cache",
    "claim": "Hashed production assets use immutable caching",
    "where": "performance contract",
    "test": "cargo test --manifest-path server/Cargo.toml --test health claim_hashed_assets_are_cached_immutably --locked",
    "sandbox": "request a hashed asset and assert one-year immutable Cache-Control"
  },
  {
    "id": "m1-operations",
    "claim": "The service exposes JSON readiness and Prometheus metrics endpoints",
    "where": "README",
    "test": "cargo test --manifest-path server/Cargo.toml --test health claim_operational_endpoints_return_readiness_and_metrics --locked",
    "sandbox": "assert /ready returns JSON 200 and /internal/metrics returns Prometheus text"
  }
]
