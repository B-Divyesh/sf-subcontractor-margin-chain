# Component inventory

The implementation contract lives in [`.factory/component-inventory.md`](../../.factory/component-inventory.md).
Build primitives in this directory, product composites under `src/features/`, and route screens under `src/routes/`.
Do not add a general-purpose card component: ledger sheets and chain rows carry specific hierarchy and semantics.

